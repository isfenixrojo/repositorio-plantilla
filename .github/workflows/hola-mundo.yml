name: hola-mundo

on: 
  workflow_dispatch: 
  # schedule:
  #   - cron: "*/5 * * * *" # Se ejecuta cada 5 minutos. 
  # push:
  #   branches: 
  #   - main

jobs:
  saludar:
    runs-on: windows-latest
    steps:
      - name: Muestra mensje hola mundo
        run: echo "Hola mundo!! Israel"
  
